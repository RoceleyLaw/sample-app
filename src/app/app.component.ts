import { Component } from '@angular/core';
import { Hero } from '../app/hero';

@Component({
  selector: 'my-app',
  template: `<h1>{{name}}</h1>
           <hero-form></hero-form>`
})

export class AppComponent  {


 }
